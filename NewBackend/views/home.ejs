<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body{
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
</head>
<body>
    <h1>URL Shortner</h1>
    <% if (locals.id) { %>

        <p>URL Generated : http://localhost:8004/bitly/<%= id %></p>
     
    <% } %>
<div>
    <form action="/bitly/link" method="post">
        <label>Enter Url Original Url</label>
        <input type="text" name="url" placeholder="enter you url"/>
        <button type="submit">Genrate</button> 
    </form>
</div>
<div>
    <% if (locals.urls) { %>
        <table>
            <thead>
                <th>Sr. No</th>
                <th>ShortID</th>
                <th>Redirect</th>
                <th>Clicks</th>
            </thead>
            <tbody>
                <% urls.forEach((url,index) => { %>
                    <tr>
                        <td><%= index+1 %></td>
                        <td><%= url.UserLink %></td>
                        <td><%= url.RedirectionId %></td>
                        <td><%= url.UserClicks.length %></td>
                    </tr>
                 
                <% }) %>
            </tbody>
        </table>
     
    <% } %>
</div>
   
</body>
</html>